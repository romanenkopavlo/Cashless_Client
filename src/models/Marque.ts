export default class Marque {
    id_marque: number;
    nom_marque: string;

    constructor(id_marque: number, nom_marque: string) {
        this.id_marque = id_marque;
        this.nom_marque = nom_marque;
    }
}